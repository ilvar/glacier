apiVersion: v1
kind: Service
metadata:
  name: {{ include "glacier.fullname" . }}
  labels:
    {{- include "glacier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "glacier.selectorLabels" . | nindent 4 }}
